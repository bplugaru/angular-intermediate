import { VerifyNameDirective } from './verify-name.directive';

describe('VerifyNameDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyNameDirective();
    expect(directive).toBeTruthy();
  });
});
